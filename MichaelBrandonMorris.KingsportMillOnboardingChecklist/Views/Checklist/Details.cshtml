@model Checklist

@{
    ViewData["Title"] = "View";
}

<h2>View</h2>

<div>
    <h4>Checklist</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.User.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.User.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.ActionItems)
        </dt>
        <dd>
            @Html.Grid(Model.ActionItems).Build(columns =>
            {
                columns.Add(model => model.Name).Titled("Action Item");
                columns.Add(model => model.Type).Titled("Type");
                columns.Add(model => model.Assignee.Name).Titled("Assignee");
            })
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>
